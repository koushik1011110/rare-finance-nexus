import React from 'react';
import SalaryManagement from '@/pages/salary/SalaryManagement';

const Payroll = () => {
  // Reuse the SalaryManagement page for payroll functionality
  return <SalaryManagement />;
};

export default Payroll;
